cuongtest As screen:

    ButtonCanvas2 As Button:
        BasePaletteColor: =
        DisplayMode: =DisplayMode.Edit
        FontColor: =
        Height: =32
        OnSelect: |-
            =RemoveIf(
                DL_HOP_DONG_SPS,
                IsBlank(MA_HOP_DONG)
            );
            RemoveIf(
                DL_HOP_DONG_SP_NDBH,
                IsBlank(MA_HOP_DONG)
            );
            RemoveIf(
                DL_HOP_DONG_SP_QLCT,
                IsBlank(MA_HOP_DONG)
            )
        Text: ="alo2"
        Width: =96
        X: =684
        Y: =376
        ZIndex: =1

